import { createStore } from 'vuex'

// Create a new store instance.
const store = createStore({
    state: {
        loading: false,
    },
    getters: {},
    actions: {
    },
    mutations: {
    },
    modules: {},
})

export default store